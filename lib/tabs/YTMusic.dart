import 'package:flutter/cupertino.dart';

class YTMusic extends StatelessWidget {
  const YTMusic({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("YT Music");
  }
}